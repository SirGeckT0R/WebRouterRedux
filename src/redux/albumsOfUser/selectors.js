export const selectAlbumsOfUser = (store) => store.albumsUser.data;

export const selectLoadingAlbumsOfUser = (store) => store.albumsUser.loading;

export const selectErrorAlbumsOfUser = (store) => store.albumsUser.error;
